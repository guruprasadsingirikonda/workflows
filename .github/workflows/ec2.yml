name: Push-to-EC2

on: push

jobs:
  deploy:
    name: Push to EC2 Instance
    runs-on: ubuntu-latest
    steps:
      - name: Create SSH key
        run: |
          apt-get sshpass
          sshpass -p 'Guru@1234'ssh testguru@18.206.160.191 
          mkdir -p guru
         
        shell: bash

   
